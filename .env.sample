# Example environment configuration for Chessmate.
# Copy this file to .env and adjust the values for your setup.

# PostgreSQL connection string used by the API/CLI and embedding worker.
DATABASE_URL=postgres://chess:chess@localhost:5433/chessmate

# OpenAI key used by the embedding worker for vector generation.
OPENAI_API_KEY=replace-with-your-openai-key

# Optional GPT-5 key for agent-based ranking (can reuse OPENAI_API_KEY).
AGENT_API_KEY=replace-with-your-gpt5-key

# Controls GPT-5 depth (minimal|low|medium|high). Defaults to medium when unset.
AGENT_REASONING_EFFORT=medium

# Optional cache capacity for GPT-5 evaluations (set to a positive integer to enable).
# AGENT_CACHE_CAPACITY=1000

# Optional per-1K token cost overrides (USD) for telemetry estimates.
# AGENT_COST_INPUT_PER_1K=0.010
# AGENT_COST_OUTPUT_PER_1K=0.030
# AGENT_COST_REASONING_PER_1K=0.060

# Optional defaults for other services.
# CHESSMATE_API_URL=http://localhost:8080
# QDRANT_URL=http://localhost:6333
